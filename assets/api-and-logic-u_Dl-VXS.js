import{i as e,r as t,s as n,t as r}from"./app-waLqxbyZ.js";var i=JSON.parse(`{"path":"/frontend/api-and-logic.html","title":"API & Candidate Logic — api.js","lang":"en-US","frontmatter":{},"git":{"updatedTime":1780776451000,"contributors":[{"name":"JaguarM","username":"JaguarM","email":"39597011+JaguarM@users.noreply.github.com","commits":4,"url":"https://github.com/JaguarM"}],"changelog":[{"hash":"d5bf1bc0899703a1340d6ae5815507072ddc8c2d","time":1780776451000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Cleanup step 3"},{"hash":"3ffbee9623e690b2489eecb53c82e380b06773a0","time":1775471641000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Fix Documentation links to GitHub URLs"},{"hash":"a267f4ad383509977e55b04d582225aecd2802d2","time":1775467276000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Updated Documentation"},{"hash":"f7330e85197db34564e6215e1d272b22e506e24e","time":1774437245000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Add redaction detection backend and docs"}]},"filePathRelative":"frontend/api-and-logic.md"}`),a={name:`api-and-logic.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="api-candidate-logic-—-api-js" tabindex="-1"><a class="header-anchor" href="#api-candidate-logic-—-api-js"><span>API &amp; Candidate Logic — <code>api.js</code></span></a></h1><p><a href="https://github.com/JaguarM/EpsteinTool/blob/main/redaction_matching/static/redaction_matching/api.js" target="_blank" rel="noopener noreferrer">api.js</a> manages the candidate name list, sends width calculation requests to the backend, renders the candidates table, and matches candidate widths against redaction box widths.</p><h2 id="per-box-format-global-pool" tabindex="-1"><a class="header-anchor" href="#per-box-format-global-pool"><span>Per-box format, global pool</span></a></h2><p>Two things are split:</p><ul><li><strong>Per box</strong> — the <strong>Name format</strong> controls (Generate / Include / Expand aliases) live on <code>box.nameSettings</code>. Each box formats names its own way and produces its own <code>box.candidates</code> (format applied to the global pool ∪ custom names). The sidebar panel edits whichever scope is <em>active</em>: the selected box, or — when nothing is selected — <code>state.nameSettings</code>, the <strong>template</strong> copied onto each new box. The <code>#name-format-scope</code> label (&quot;· this box&quot; / &quot;· new boxes&quot;) shows which; <code>syncNameSettingsUI()</code> pushes the active settings into the checkboxes on select/deselect.</li><li><strong>Global</strong> — the underlying <em>people pool</em>. <code>state.customCandidates</code> (added names) and <code>state.excludedPersons</code> (deleted people, by <code>namesData</code> index) are shared across every box. Adding and deleting operate on this pool, so a name shows up in / disappears from <strong>all</strong> boxes at once.</li></ul><p><code>generateCandidatesFromData(namesData, settings, { excluded, ownerMap })</code> skips <code>excluded</code> person indices and, when given an <code>ownerMap</code>, records which person produced each string so a deleted row can be traced back to a person.</p><p><code>state.candidates</code> is not the matching source — it is the template union (template settings ∪ custom, minus deleted) used only by the uppercase heuristic in <code>embedded-text-viewer.js</code>. Per-box matching reads <code>box.candidates</code> via <code>getBoxCandidates(box)</code>.</p><h2 id="candidate-management" tabindex="-1"><a class="header-anchor" href="#candidate-management"><span>Candidate Management</span></a></h2><h3 id="addname-processpaste" tabindex="-1"><a class="header-anchor" href="#addname-processpaste"><span><code>addName()</code> / <code>processPaste()</code></span></a></h3><p>Adds names to <code>state.customCandidates</code> (global), then rebuilds every box&#39;s <code>candidates</code> and recalculates widths.</p><h3 id="clearall" tabindex="-1"><a class="header-anchor" href="#clearall"><span><code>clearAll()</code></span></a></h3><p>Prompts confirmation, then clears the custom names <strong>and</strong> the deleted-people set (restoring deletions), and rebuilds every box.</p><h3 id="removename-name" tabindex="-1"><a class="header-anchor" href="#removename-name"><span><code>removeName(name)</code></span></a></h3><p>Global delete. Traces the clicked row — which may be just a first or last name — back to the whole person via the selected box&#39;s owner map, adds that person to <code>state.excludedPersons</code>, drops the name from the custom list if present, then rebuilds every box. The whole name disappears from every box regardless of each box&#39;s format.</p><h2 id="width-calculation" tabindex="-1"><a class="header-anchor" href="#width-calculation"><span>Width Calculation</span></a></h2><h3 id="calculateallwidths" tabindex="-1"><a class="header-anchor" href="#calculateallwidths"><span><code>calculateAllWidths()</code></span></a></h3><p>Sends one <code>POST /widths</code> request per redaction (in parallel via <code>Promise.all</code>), using each redaction&#39;s individual <code>settings</code> (font, size, scale, kerning).</p><h3 id="calculatewidthsforredaction-idx" tabindex="-1"><a class="header-anchor" href="#calculatewidthsforredaction-idx"><span><code>calculateWidthsForRedaction(idx)</code></span></a></h3><p>Sends a single width calculation request for the candidate list using the settings of redaction <code>idx</code>. Stores results in <code>redaction.widths[candidateName] = measuredWidth</code>.</p><h2 id="rendering" tabindex="-1"><a class="header-anchor" href="#rendering"><span>Rendering</span></a></h2><h3 id="rendercandidates" tabindex="-1"><a class="header-anchor" href="#rendercandidates"><span><code>renderCandidates()</code></span></a></h3><p>Renders the paginated candidates table in the sidebar. Shows name, measured width, and a delete button. Supports sorting by name or width.</p><h3 id="selectredaction-idx" tabindex="-1"><a class="header-anchor" href="#selectredaction-idx"><span><code>selectRedaction(idx)</code></span></a></h3><p>Selects a redaction by index: navigates to its page, updates the settings controls, highlights the redaction overlay and the match table row, then re-renders candidates.</p><h3 id="updateallmatchesview-onlyidx" tabindex="-1"><a class="header-anchor" href="#updateallmatchesview-onlyidx"><span><code>updateAllMatchesView(onlyIdx?)</code></span></a></h3><p>Re-renders the &quot;All Matches&quot; table. For each redaction, finds candidates whose width is within the tolerance. Updates the overlay label text (unless manually overridden). Shows match count summary.</p><h2 id="helper" tabindex="-1"><a class="header-anchor" href="#helper"><span>Helper</span></a></h2><h3 id="getfontfamily-fontname" tabindex="-1"><a class="header-anchor" href="#getfontfamily-fontname"><span><code>getFontFamily(fontName)</code></span></a></h3><p>Maps font filenames to CSS font-family strings:</p><ul><li><code>times</code> → <code>&quot;Times New Roman&quot;, serif</code></li><li><code>arial</code> → <code>Arial, sans-serif</code></li><li><code>calibri</code> → <code>Calibri, sans-serif</code></li><li><code>cour</code> → <code>&quot;Courier New&quot;, monospace</code></li></ul>`,30)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};