import{a as e,c as t,i as n,l as r,n as i,o as a,r as o,s,t as c}from"./app-DqptZ8gJ.js";var l=JSON.parse(`{"path":"/redaction-processing/surrounding-word-width.html","title":"SurroundingWordWidth.py Documentation","lang":"en-US","frontmatter":{},"git":{"updatedTime":1781445385000,"contributors":[{"name":"JaguarM","username":"JaguarM","email":"39597011+JaguarM@users.noreply.github.com","commits":4,"url":"https://github.com/JaguarM"}],"changelog":[{"hash":"2d6dab65bfee9addf95c5fc7983ea9fe88712e13","time":1781445385000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"update docs"},{"hash":"3ffbee9623e690b2489eecb53c82e380b06773a0","time":1775471641000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Fix Documentation links to GitHub URLs"},{"hash":"a267f4ad383509977e55b04d582225aecd2802d2","time":1775467276000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Updated Documentation"},{"hash":"f7330e85197db34564e6215e1d272b22e506e24e","time":1774437245000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Add redaction detection backend and docs"}]},"filePathRelative":"redaction-processing/surrounding-word-width.md"}`),u={name:`surrounding-word-width.md`};function d(c,l,u,d,f,p){let m=t(`RouteLink`);return s(),o(`div`,null,[l[30]||=n(`<h1 id="surroundingwordwidth-py-documentation" tabindex="-1"><a class="header-anchor" href="#surroundingwordwidth-py-documentation"><span>SurroundingWordWidth.py Documentation</span></a></h1><p><a href="../../guesser_core/logic/SurroundingWordWidth.py"><code>guesser_core/logic/SurroundingWordWidth.py</code></a> refines the <strong>horizontal edges</strong> of detected redaction boxes by reconstructing where the hidden text actually started and ended in the original Microsoft Word source. It combines two kinds of evidence:</p><ol><li><strong>Pixel evidence</strong> — the painted box edge, any glyph of the hidden text that pokes past the paint, and the near edge of the neighbouring visible word.</li><li><strong>Word-grid evidence</strong> — Word lays text on a predictable grid (¼-inch indents and tab stops, a 1-inch default margin) and stretches inter-word spaces on justified lines. The left edge is snapped/spaced to match that grid.</li></ol>`,3),i(`p`,null,[l[2]||=e(`It is wrapped by the `,-1),l[3]||=i(`a`,{href:`../../redaction_refiner/etv_refiner.py`},[i(`code`,null,`EtvRefiner`)],-1),l[4]||=e(` plugin; the core orchestrator never calls it directly (see `,-1),a(m,{to:`/redaction-processing/process-redactions-docs.html`},{default:r(()=>[...l[0]||=[e(`ProcessRedactions`,-1)]]),_:1}),l[5]||=e(` and the refiner architecture in `,-1),a(m,{to:`/architecture/architecture-overview.html`},{default:r(()=>[...l[1]||=[e(`architecture-overview`,-1)]]),_:1}),l[6]||=e(`).`,-1)]),l[31]||=n(`<p>All canonical geometry is <strong>image pixels @ 96 DPI</strong>. The constants (<code>GRID_PX = 24</code>, <code>DEFAULT_MARGIN_PX = 96</code>, <code>JUSTIFY_SPACE_TOL_PX</code>, the gap-filter ranges) live in <a href="../../guesser_core/logic/geometry.py"><code>geometry.py</code></a>.</p><hr><h2 id="core-function" tabindex="-1"><a class="header-anchor" href="#core-function"><span>Core Function</span></a></h2><h3 id="estimate-widths-for-boxes-page-boxes-img-rect-img-w-img-h-base-image-bytes-none-debug-out-none" tabindex="-1"><a class="header-anchor" href="#estimate-widths-for-boxes-page-boxes-img-rect-img-w-img-h-base-image-bytes-none-debug-out-none"><span><code>estimate_widths_for_boxes(page, boxes, img_rect, img_w, img_h, base_image_bytes=None, debug_out=None)</code></span></a></h3><p><strong>Inputs:</strong></p><ul><li><code>page</code> — a <code>fitz.Page</code> (PyMuPDF); used for <code>get_text(&quot;words&quot;)</code> and <code>get_text(&quot;dict&quot;)</code> (font/size of each line).</li><li><code>boxes</code> — list of <code>(x1, y1, x2, y2)</code> tuples in <strong>image pixel</strong> coordinates (from BoxDetector).</li><li><code>img_rect</code> — <code>fitz.Rect</code> of the embedded image on the PDF page (PDF points).</li><li><code>img_w</code>, <code>img_h</code> — pixel dimensions of the source image.</li><li><code>base_image_bytes</code> — optional raw PNG bytes. When present, edges are validated and refined against the actual pixels; when absent, pixel scans degrade to the raw box edge.</li><li><code>debug_out</code> — optional list. When provided, one dict of per-box intermediates is appended for each input box (see <a href="#debug-output">Debug output</a>). Default <code>None</code> keeps production cost unchanged; only the width debugger passes it.</li></ul><p><strong>Output:</strong> A list of <code>(expected_x1, expected_x2, expected_height)</code> tuples, one per input box, all in image pixels. Any element is <code>None</code> when that value could not be reconstructed. <code>expected_height</code> is the line&#39;s mean word height (used elsewhere for vertical snapping); the refiner consumes only <code>expected_x1</code>/<code>expected_x2</code>.</p><hr><h2 id="algorithm" tabindex="-1"><a class="header-anchor" href="#algorithm"><span>Algorithm</span></a></h2><h3 id="_1-coordinate-conversion" tabindex="-1"><a class="header-anchor" href="#_1-coordinate-conversion"><span>1. Coordinate conversion</span></a></h3><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line">px_to_pts_x <span class="token operator">=</span> img_rect<span class="token punctuation">.</span>width <span class="token operator">/</span> img_w        <span class="token comment"># 0.75 on a standard 612pt / 816px page</span></span>
<span class="line">pts_to_px_x <span class="token operator">=</span> <span class="token number">1.0</span> <span class="token operator">/</span> px_to_pts_x             <span class="token comment"># 1.333…</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Each box is converted to point coordinates for comparison against the text layer. The page text layer (<code>get_text(&quot;words&quot;)</code>) is read once, as is a flattened list of spans (<code>get_text(&quot;dict&quot;)</code>) used to recover each line&#39;s font and size.</p><h3 id="_2-word-bucketing-by-line" tabindex="-1"><a class="header-anchor" href="#_2-word-bucketing-by-line"><span>2. Word bucketing by line</span></a></h3><p>For each box, the page words are grouped into horizontal &quot;line buckets&quot;:</p><ul><li>a word joins the box&#39;s line if <strong>≥ 50 %</strong> of its height overlaps the box vertically;</li><li>words within <strong>5 pts</strong> of the same y-midpoint share a bucket.</li></ul><blockquote><p>The buckets include words sitting <strong>under</strong> the redaction — the pixel paint hides the ink but the PDF text layer survives — which is what makes the justification rewrite (step 4) possible.</p></blockquote><h3 id="_3-nearest-words-before-after" tabindex="-1"><a class="header-anchor" href="#_3-nearest-words-before-after"><span>3. Nearest words (before / after)</span></a></h3><p>In each bucket the algorithm picks:</p><ul><li><strong><code>word_before</code></strong> — the word whose right edge is closest to, and left of, the box&#39;s left edge;</li><li><strong><code>word_after</code></strong> — the word whose left edge is closest to, and right of, the box&#39;s right edge.</li></ul><p>A neighbour is rejected if another redaction box lies between it and the current box on the same line (<strong>obstruction detection</strong>), so a measurement never reaches through an intervening redaction. The best bucket maximises the match count (prefer both sides), then minimises total distance.</p><p>If <strong>no</strong> bucket yields a neighbour, the box is <em>isolated</em> — see <a href="#isolated-boxes-no-line-context">Isolated boxes</a>.</p><h3 id="_4-space-reconstruction-the-word-grid" tabindex="-1"><a class="header-anchor" href="#_4-space-reconstruction-the-word-grid"><span>4. Space reconstruction (the Word grid)</span></a></h3><p>Three space-related quantities are computed for the chosen line:</p>`,23),i(`table`,null,[l[16]||=i(`thead`,null,[i(`tr`,null,[i(`th`,null,`Quantity`),i(`th`,null,`Source`),i(`th`,null,`Used for`)])],-1),i(`tbody`,null,[l[14]||=i(`tr`,null,[i(`td`,null,[i(`code`,null,`measured_gap`)]),i(`td`,null,[e(`mean of inter-word pixel gaps kept inside `),i(`code`,null,`GAP_FILTER_RANGE`),e(` (3–11 px)`)]),i(`td`,null,`justification test; the stretched space on a justified line`)],-1),i(`tr`,null,[l[12]||=i(`td`,null,[i(`code`,null,`natural_space`)],-1),i(`td`,null,[l[8]||=e(`HarfBuzz-shaped advance of `,-1),l[9]||=i(`code`,null,`" "`,-1),l[10]||=e(` in the line's font/size (via `,-1),a(m,{to:`/redaction-processing/width-calculator-documentation.html`},{default:r(()=>[...l[7]||=[i(`code`,null,`width_calculator.get_text_widths`,-1)]]),_:1}),l[11]||=e(`)`,-1)]),l[13]||=i(`td`,null,`the space after a word on a normal line`,-1)]),l[15]||=i(`tr`,null,[i(`td`,null,[i(`code`,null,`space_px`)]),i(`td`,null,[i(`code`,null,`measured_gap`),e(` clamped to `),i(`code`,null,`SPACE_PX_CLAMP`),e(` (3–8 px), `),i(`code`,null,`SPACE_PX_FALLBACK`),e(` (5 px) if none`)]),i(`td`,null,[e(`the `),i(`strong`,null,`right`),e(` edge, and the fallback when shaping is unavailable`)])],-1)])]),l[32]||=n(`<p><strong>Justification test.</strong> A line is treated as justified when the <em>measured</em> gap exceeds the font&#39;s natural space by more than <code>JUSTIFY_SPACE_TOL_PX</code>:</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line">is_justified <span class="token operator">=</span> <span class="token punctuation">(</span>measured_gap <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span> <span class="token keyword">and</span> natural_space <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span></span>
<span class="line">                <span class="token keyword">and</span> measured_gap <span class="token operator">&gt;</span> natural_space <span class="token operator">+</span> geo<span class="token punctuation">.</span>JUSTIFY_SPACE_TOL_PX<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The space used for the <strong>left</strong> edge is then:</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line"><span class="token keyword">if</span> is_justified<span class="token punctuation">:</span>    word_space_px <span class="token operator">=</span> measured_gap     <span class="token comment"># stretched (bounded by GAP_FILTER_RANGE)</span></span>
<span class="line"><span class="token keyword">elif</span> natural_space<span class="token punctuation">:</span> word_space_px <span class="token operator">=</span> natural_space     <span class="token comment"># one natural space</span></span>
<span class="line"><span class="token keyword">else</span><span class="token punctuation">:</span>               word_space_px <span class="token operator">=</span> space_px          <span class="token comment"># pixel-gap fallback</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>Why the measured gap, not a fill-width solve?</strong> <code>width_calculator</code> also exposes <code>get_justified_space_width()</code> (solve for the per-space width that makes the line fill its block). In this corpus the text layer <em>under</em> a redaction is often fragmented, which makes that solve explode (e.g. a 40 px &quot;space&quot; against a 3.8 px natural space) and can push the edge <em>into</em> the box. The measured inter-word gap between real visible words is bounded and reliable, so the solve is computed for the debug stream only and does <strong>not</strong> drive placement.</p></blockquote><p><strong>First-word grid offset.</strong> In Word the line&#39;s first word (the paragraph origin) sits on a ¼-inch grid line. Its left edge is snapped to the grid and the resulting correction is carried along the line:</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line">first_word_x0_px <span class="token operator">=</span> <span class="token punctuation">(</span>best_line_words<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span> <span class="token operator">-</span> img_rect<span class="token punctuation">.</span>x0<span class="token punctuation">)</span> <span class="token operator">*</span> pts_to_px_x</span>
<span class="line">grid_origin_px   <span class="token operator">=</span> <span class="token builtin">round</span><span class="token punctuation">(</span>first_word_x0_px <span class="token operator">/</span> geo<span class="token punctuation">.</span>GRID_PX<span class="token punctuation">)</span> <span class="token operator">*</span> geo<span class="token punctuation">.</span>GRID_PX</span>
<span class="line">grid_offset      <span class="token operator">=</span> grid_origin_px <span class="token operator">-</span> first_word_x0_px        <span class="token comment"># bounded to ±½ grid cell</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_5-left-edge-placement" tabindex="-1"><a class="header-anchor" href="#_5-left-edge-placement"><span>5. Left-edge placement</span></a></h3><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line">content_x1 <span class="token operator">=</span> _content_edge<span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span>          <span class="token comment"># box ink + any poking glyph</span></span>
<span class="line">nbr_l      <span class="token operator">=</span> _next_word_edge<span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span>         <span class="token comment"># previous word&#39;s far edge, by pixels</span></span>
<span class="line">grid_x1    <span class="token operator">=</span> <span class="token builtin">round</span><span class="token punctuation">(</span>content_x1 <span class="token operator">/</span> geo<span class="token punctuation">.</span>GRID_PX<span class="token punctuation">)</span> <span class="token operator">*</span> geo<span class="token punctuation">.</span>GRID_PX</span>
<span class="line"></span>
<span class="line"><span class="token keyword">if</span> word_before <span class="token keyword">is</span> <span class="token boolean">None</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token comment"># No text in front: the box itself is the first thing on the line, so its</span></span>
<span class="line">    <span class="token comment"># true start is a grid line. Snap the painted edge to the nearest grid line</span></span>
<span class="line">    <span class="token comment"># (origin = page left edge). The render can drift; the grid matches the source.</span></span>
<span class="line">    expected_x1 <span class="token operator">=</span> grid_x1</span>
<span class="line"><span class="token keyword">else</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token comment"># Text in front: start one (natural or stretched) space after that word&#39;s</span></span>
<span class="line">    <span class="token comment"># far edge, re-anchored to the grid by the line&#39;s first-word offset. max()</span></span>
<span class="line">    <span class="token comment"># never lets the result sit inside real ink (keeps a poking glyph).</span></span>
<span class="line">    expected_x1 <span class="token operator">=</span> content_x1</span>
<span class="line">    <span class="token keyword">if</span> nbr_l <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token boolean">None</span><span class="token punctuation">:</span></span>
<span class="line">        expected_x1 <span class="token operator">=</span> <span class="token builtin">max</span><span class="token punctuation">(</span>content_x1<span class="token punctuation">,</span> nbr_l <span class="token operator">+</span> word_space_px <span class="token operator">+</span> grid_offset<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The result is <em>not</em> grid-snapped in the word-before case: only the line&#39;s first word is grid-aligned in Word; a mid-line hidden word is positioned by spacing relative to that origin.</p><h3 id="_6-right-edge-placement" tabindex="-1"><a class="header-anchor" href="#_6-right-edge-placement"><span>6. Right-edge placement</span></a></h3><p>Unchanged by the grid work — the right edge is <code>min(content_x2, nbr_r - space_px)</code> when a following word exists, else the painted content edge.</p><h3 id="isolated-boxes-no-line-context" tabindex="-1"><a class="header-anchor" href="#isolated-boxes-no-line-context"><span>Isolated boxes (no line context)</span></a></h3><p>When step 3 finds no neighbour on any candidate line, the box is isolated. Rather than abstaining, the <strong>left</strong> edge is still snapped to the grid (an isolated redaction in Word also began on a grid line), and the right edge abstains:</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line">content_x1  <span class="token operator">=</span> _content_edge<span class="token punctuation">(</span>img<span class="token punctuation">,</span> bx1<span class="token punctuation">,</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span> y0_scan<span class="token punctuation">,</span> y1_scan<span class="token punctuation">,</span> <span class="token number">0.0</span><span class="token punctuation">)</span></span>
<span class="line">expected_x1 <span class="token operator">=</span> <span class="token builtin">round</span><span class="token punctuation">(</span>content_x1 <span class="token operator">/</span> geo<span class="token punctuation">.</span>GRID_PX<span class="token punctuation">)</span> <span class="token operator">*</span> geo<span class="token punctuation">.</span>GRID_PX        <span class="token comment"># nearest grid line</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="pixel-edge-helpers" tabindex="-1"><a class="header-anchor" href="#pixel-edge-helpers"><span>Pixel-edge helpers</span></a></h2><p>These resolve the painted edge and the neighbouring word from the raster. Each takes the grayscale image, scans a vertical band inset by 2 px from the box top and bottom, and is bounded so it can never cross into a neighbouring word.</p><table><thead><tr><th>Helper</th><th>Role</th></tr></thead><tbody><tr><td><code>_is_glyph_col(col)</code></td><td>True if a column looks like letter ink (darkens some rows) rather than a full-height box edge or blank paper.</td></tr><tr><td><code>_content_edge(img, ink_edge, dir, y0, y1, bound)</code></td><td>Walk outward through contiguous glyph ink to absorb a glyph that pokes past the paint; otherwise recover the box&#39;s own anti-aliased fringe.</td></tr><tr><td><code>_subpixel_glyph_edge(...)</code></td><td>Refine an absorbed poke to its sub-pixel 50 %-ink crossing.</td></tr><tr><td><code>_box_aa_edge(...)</code></td><td>When no glyph pokes, extend the edge across the box&#39;s anti-aliased fringe that BoxDetector trimmed.</td></tr><tr><td><code>_next_word_edge(img, start, dir, y0, y1, bound)</code></td><td>Scan across the inter-word whitespace to the <strong>pixel</strong> near-edge of the next word — found from ink because the text layer under a box may report only a fragment.</td></tr></tbody></table><h2 id="font-helpers" tabindex="-1"><a class="header-anchor" href="#font-helpers"><span>Font helpers</span></a></h2><table><thead><tr><th>Helper</th><th>Role</th></tr></thead><tbody><tr><td><code>_collect_spans(page)</code></td><td>Flatten <code>page.get_text(&quot;dict&quot;)</code> to <code>(bbox, font_name, size_pt)</code>.</td></tr><tr><td><code>_line_font(spans, line_words)</code></td><td>Dominant <code>(ttf, size_pt)</code> for the spans covering a line.</td></tr><tr><td><code>_map_font(pdf_font_name)</code></td><td>Map a PDF base-font name (e.g. <code>TimesNewRomanPSMT</code>) to a TTF in <code>assets/fonts/</code>; defaults to <code>times.ttf</code>.</td></tr><tr><td><code>_line_space_widths(...)</code></td><td>Returns <code>(natural_space, justified_space)</code>; the justified value is debug-only (see step 4 note).</td></tr></tbody></table><hr><h2 id="debug-output" tabindex="-1"><a class="header-anchor" href="#debug-output"><span>Debug output</span></a></h2><p>When <code>debug_out</code> is passed, each matched box appends a dict with the raw and reconstructed intermediates:</p><p><code>raw_ink</code>, <code>line_words</code>, <code>word_before</code>, <code>word_after</code>, <code>left_bound</code>, <code>right_bound</code>, <code>space_px</code>, <code>gaps</code>, <code>y_scan</code>, <code>content_x1</code>, <code>content_x2</code>, <code>nbr_l</code>, <code>nbr_r</code>, <code>expected_x1</code>, <code>expected_x2</code>, and the Word-grid fields: <code>font</code>, <code>size_pt</code>, <code>block_w_px</code>, <code>measured_gap</code>, <code>natural_space</code>, <code>justified_space</code>, <code>is_justified</code>, <code>word_space_px</code>, <code>grid_x1</code>, <code>first_word_x0_px</code>, <code>grid_origin_px</code>, <code>grid_offset</code>, <code>word_before_present</code>.</p>`,25),i(`p`,null,[l[18]||=e(`Isolated boxes append `,-1),l[19]||=i(`code`,null,`reason`,-1),l[20]||=e(`, `,-1),l[21]||=i(`code`,null,`content_x1`,-1),l[22]||=e(`, `,-1),l[23]||=i(`code`,null,`grid_x1`,-1),l[24]||=e(`, `,-1),l[25]||=i(`code`,null,`expected_x1`,-1),l[26]||=e(`, `,-1),l[27]||=i(`code`,null,`word_before_present=False`,-1),l[28]||=e(`. See `,-1),a(m,{to:`/redaction-processing/box-edge-debugging-prompt.html`},{default:r(()=>[...l[17]||=[e(`box-edge-debugging-prompt`,-1)]]),_:1}),l[29]||=e(` for the harness.`,-1)]),l[33]||=n(`<hr><h2 id="constraints-parameters" tabindex="-1"><a class="header-anchor" href="#constraints-parameters"><span>Constraints &amp; parameters</span></a></h2><table><thead><tr><th>Parameter</th><th>Source</th><th>Value</th><th>Purpose</th></tr></thead><tbody><tr><td>Vertical overlap threshold</td><td>inline</td><td>50 %</td><td>min word-box vertical overlap to share a line</td></tr><tr><td>Line bucket tolerance</td><td>inline</td><td>5 pts</td><td>max y-midpoint distance to group words</td></tr><tr><td><code>GAP_FILTER_RANGE</code></td><td>geometry.py</td><td>3–11 px</td><td>raw inter-word gaps kept before averaging</td></tr><tr><td><code>SPACE_PX_CLAMP</code></td><td>geometry.py</td><td>3–8 px</td><td>clamp on the per-line mean gap (right edge / fallback)</td></tr><tr><td><code>SPACE_PX_FALLBACK</code></td><td>geometry.py</td><td>5 px</td><td>mean gap assumed when none is measurable</td></tr><tr><td><code>GRID_PX</code></td><td>geometry.py</td><td>24 px</td><td>¼-inch Word grid step (origin = page left edge)</td></tr><tr><td><code>DEFAULT_MARGIN_PX</code></td><td>geometry.py</td><td>96 px</td><td>Word&#39;s default 1-inch left margin (grid index 4)</td></tr><tr><td><code>JUSTIFY_SPACE_TOL_PX</code></td><td>geometry.py</td><td>1.5 px</td><td>per-space stretch over natural that marks a justified line</td></tr></tbody></table><hr><h2 id="downstream" tabindex="-1"><a class="header-anchor" href="#downstream"><span>Downstream</span></a></h2><p><code>EtvRefiner</code> turns the returned edges into a <code>BoxProposal(x, x2, confidence=0.95, source=&quot;etv&quot;)</code>. Its width guard rejects an edge only when the width change exceeds <strong><code>max(25 % of the box width, GRID_PX)</code></strong> — the one-grid-cell floor means a bounded grid snap on a small box is never silently dropped, while a wild ETV expansion is still caught. The pipeline merges edges by confidence (highest wins; ties prefer the wider box), so the 0.95 grid/justification edge overrides a lower-confidence pixel edge.</p><h2 id="known-limitations-future-work" tabindex="-1"><a class="header-anchor" href="#known-limitations-future-work"><span>Known limitations / future work</span></a></h2><ul><li><strong>Wide justified spaces</strong> beyond <code>GAP_FILTER_RANGE</code> (11 px) are filtered out, so a heavily justified line can under-stretch. Raising the cap risks admitting box-spanning gaps; a justified-aware upper bound would be safer.</li><li><strong>Isolated/no-word snap uses <em>nearest</em> grid line</strong>, which can move the edge right and (slightly) reveal hidden ink. A &quot;nearest at-or-left&quot; mode would be safer for live redaction at the cost of grid fidelity.</li><li><strong>First word redacted</strong>: if the line&#39;s first word is itself a fragment under a box, <code>grid_offset</code> is computed from a wrong baseline (still bounded to ±½ cell).</li><li><strong>Grid origin is the page edge</strong>, which coincides with a 1-inch margin (96 is a multiple of 24). A document with a non-grid margin would need detected-margin origin instead.</li></ul>`,8)])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};