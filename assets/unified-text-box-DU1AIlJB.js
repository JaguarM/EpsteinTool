import{a as e,c as t,i as n,l as r,n as i,o as a,r as o,s,t as c}from"./app-BvVgHaVZ.js";var l=JSON.parse(`{"path":"/architecture/unified-text-box.html","title":"Unified Text Box System","lang":"en-US","frontmatter":{},"git":{"updatedTime":1778318743000,"contributors":[{"name":"JaguarM","username":"JaguarM","email":"39597011+JaguarM@users.noreply.github.com","commits":1,"url":"https://github.com/JaguarM"}],"changelog":[{"hash":"397520d0fe3de3dbcbf9c4d3713d731692ecb096","time":1778318743000,"email":"39597011+JaguarM@users.noreply.github.com","author":"JaguarM","message":"Add Unified Text Box docs and rename guides"}]},"filePathRelative":"architecture/unified-text-box.md"}`),u={name:`unified-text-box.md`};function d(c,l,u,d,f,p){let m=t(`RouteLink`);return s(),o(`div`,null,[l[5]||=i(`h1`,{id:`unified-text-box-system`,tabindex:`-1`},[i(`a`,{class:`header-anchor`,href:`#unified-text-box-system`},[i(`span`,null,`Unified Text Box System`)])],-1),l[6]||=i(`p`,null,`All text on a page — extracted PDF text, redaction labels, and HarfBuzz recreations — is stored and rendered through a single data model and a single rendering pipeline. There are no separate state objects for spans, redactions, or HarfBuzz overlays.`,-1),i(`p`,null,[l[1]||=e(`See `,-1),a(m,{to:`/frontend/embedded-text-viewer.html`},{default:r(()=>[...l[0]||=[e(`embedded-text-viewer.md`,-1)]]),_:1}),l[2]||=e(` for the full `,-1),l[3]||=i(`code`,null,`UnifiedTextBox`,-1),l[4]||=e(` field reference and SVG rendering internals.`,-1)]),l[7]||=n(`<hr><h2 id="box-types" tabindex="-1"><a class="header-anchor" href="#box-types"><span>Box Types</span></a></h2><table><thead><tr><th><code>type</code></th><th>Origin</th><th>Role</th></tr></thead><tbody><tr><td><code>embedded</code></td><td>PDF extractor (<code>/api/extract-spans</code>)</td><td>Ground-truth text; not user-editable via the text tool</td></tr><tr><td><code>redaction</code></td><td>User draws a box on the page</td><td>Label text managed by the match engine; snaps font/size to nearest <code>embedded</code> box</td></tr><tr><td><code>harfbuzz</code></td><td>Inspector tool</td><td>Transient overlay that visualises HarfBuzz-computed layout; used to diagnose spacing errors</td></tr></tbody></table><p>Each type renders with a distinct colour (defined in <code>svg-renderer.js::UTB_TYPE_COLORS</code>):</p><ul><li><code>embedded</code> — blue</li><li><code>redaction</code> — green</li><li><code>harfbuzz</code> — orange</li></ul><hr><h2 id="module-reference" tabindex="-1"><a class="header-anchor" href="#module-reference"><span>Module Reference</span></a></h2><p>All five modules live in <code>text_tool/static/text_tool/</code>.</p><table><thead><tr><th>Module</th><th>Responsibility</th><th>Key exports</th></tr></thead><tbody><tr><td><code>unified-text-box.js</code></td><td><code>UnifiedTextBox</code> class and <code>utbState</code> global</td><td><code>utbState.addBox()</code>, <code>getBox()</code>, <code>updateBox()</code>, <code>getPageBoxes()</code>, <code>reset()</code></td></tr><tr><td><code>svg-renderer.js</code></td><td>Renders boxes as SVG <code>&lt;text&gt;</code> elements in a per-page layer</td><td><code>renderBox(box)</code>, <code>renderTextLayer(pageContainer, pageNum)</code>, <code>renderAllTextLayers()</code>, <code>computeXPositions(box)</code>, <code>computeBaseline(box)</code></td></tr><tr><td><code>toolbar.js</code></td><td>Unified formatting toolbar — one code path for all box types</td><td><code>syncToolbarToBox(box)</code>, <code>syncToolbarToSelection()</code></td></tr><tr><td><code>micro-typo.js</code></td><td>Per-character nudge mode via hit-rects and a popover slider</td><td><code>enterMicroTypo(box)</code>, <code>exitMicroTypo()</code></td></tr><tr><td><code>inline-edit.js</code></td><td>Double-click WYSIWYG editing for <code>embedded</code> and <code>harfbuzz</code> boxes</td><td><code>enterInlineEdit(box)</code>, <code>commitInlineEdit()</code>, <code>cancelInlineEdit()</code></td></tr></tbody></table><hr><h2 id="rendering-pipeline" tabindex="-1"><a class="header-anchor" href="#rendering-pipeline"><span>Rendering Pipeline</span></a></h2><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">utbState.boxes</span>
<span class="line">    │</span>
<span class="line">    ▼</span>
<span class="line">renderBox(box)            ← called by renderTextLayer() / renderAllTextLayers()</span>
<span class="line">    │</span>
<span class="line">    ├─ computeBaseline(box)          baseline y in SVG coordinate space</span>
<span class="line">    │</span>
<span class="line">    └─ computeXPositions(box)        absolute x array for SVG &lt;text x=&quot;…&quot;&gt;</span>
<span class="line">           │</span>
<span class="line">           ├─ box.baseCharPositions  per-char offsets from PDF extraction / HarfBuzz</span>
<span class="line">           ├─ box.charAdvances[i]    accumulated per-char nudge deltas (micro-typo)</span>
<span class="line">           └─ box.spaceWidth         manual word-spacing override (when defaultSpaceWidth=false)</span>
<span class="line">    │</span>
<span class="line">    ▼</span>
<span class="line">SVG &lt;text&gt; element in .text-layer[data-page=&quot;N&quot;]</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The SVG layer uses a fixed <code>viewBox</code> matching document pixel space (816 × 1056). Zoom is handled entirely by CSS sizing on the layer element — coordinate values in <code>box.x/y/w/h</code> never change.</p><hr><h2 id="interactions" tabindex="-1"><a class="header-anchor" href="#interactions"><span>Interactions</span></a></h2><h3 id="toolbar" tabindex="-1"><a class="header-anchor" href="#toolbar"><span>Toolbar</span></a></h3><p>Clicking any box activates the formatting toolbar. <code>syncToolbarToBox(box)</code> pushes the box&#39;s properties into the UI; every control writes back directly to the <code>UnifiedTextBox</code> instance via <code>utbState.updateBox()</code>. There is no branching on <code>box.type</code>.</p><h3 id="space-width" tabindex="-1"><a class="header-anchor" href="#space-width"><span>Space Width</span></a></h3><p>Each box has an independent <code>defaultSpaceWidth</code> boolean and <code>spaceWidth</code> float.</p><ul><li><code>defaultSpaceWidth = true</code> — the box uses the font&#39;s native space advance (<code>nativeSpaceWidth</code>, cached from HarfBuzz).</li><li><code>defaultSpaceWidth = false</code> — the slider is active; <code>spaceWidth</code> overrides the space width and <code>computeXPositions</code> shifts all characters after each space accordingly.</li></ul><h3 id="inline-text-editing" tabindex="-1"><a class="header-anchor" href="#inline-text-editing"><span>Inline Text Editing</span></a></h3><p>Double-clicking an <code>embedded</code> or <code>harfbuzz</code> box calls <code>enterInlineEdit(box)</code>, which places a <code>&lt;foreignObject&gt;</code> overlay containing a styled <code>&lt;input&gt;</code> that matches the box&#39;s font, size, weight, style, and colour.</p><ul><li><code>Enter</code> or click-away → <code>commitInlineEdit()</code> saves <code>box.text</code> and re-renders.</li><li><code>Escape</code> → <code>cancelInlineEdit()</code> discards changes.</li></ul><p><code>redaction</code> boxes are excluded — their <code>labelText</code> is managed by the match engine.</p><h3 id="micro-typography-nudge-mode" tabindex="-1"><a class="header-anchor" href="#micro-typography-nudge-mode"><span>Micro-Typography (Nudge Mode)</span></a></h3><p>Clicking the Nudge button (↔) in the toolbar calls <code>enterMicroTypo(box)</code>. The renderer draws invisible hit-rects over each character. Clicking a character opens a popover slider that writes a delta (in px) to <code>box.charAdvances[charIndex]</code>. <code>computeXPositions</code> accumulates all prior deltas so nudging character <em>i</em> also shifts characters <em>i+1, i+2, …</em> — matching the SVG <code>&lt;text x=&quot;…&quot;&gt;</code> array contract.</p>`,26)])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};